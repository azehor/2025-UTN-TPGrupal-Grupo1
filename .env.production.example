# Ejemplo de variables para PRODUCCIÓN 
# Copiar como .env.production y ajustar según tu entorno

# --- Ejecución de la API ---
APP_ENV=production
APP_HOST=0.0.0.0
APP_PORT=8080

# --- Conexión a la base de datos (usada por la API) ---
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_NAME=db_nahue
DB_SSLMODE=disable
# En producción se recomienda leer la contraseña desde un archivo secreto dentro del contenedor
DB_PASSWORD_FILE=/run/secrets/db_password
# O si no usás secrets (no recomendado):
# DB_PASSWORD=contraseña_fuerte_produccion

# --- Contenedor de Postgres (servicio db) ---
POSTGRES_USER=postgres
POSTGRES_DB=db_nahue
POSTGRES_PASSWORD_FILE=/run/secrets/db_password

# --- ARG para build del Frontend ---
VITE_API_BASE_URL=/v1

# --- Assets estáticos ---
# Base pública para construir URLs de /static
API_ASSETS_BASE_URL=http://quepc.home

# --- Traefik ---
# Puerto expuesto en el host para la entrada web de Traefik
TRAEFIK_PORT=8080

# --- JWT  ---
JWT_TTL_MIN=30
# El secreto va en .env.production.secrets.example

