# Ejemplo de secretos para PRODUCCIÓN (copiar como .env.production.secrets y ajustar)

# Preferir usar archivos de secrets de Docker dentro del contenedor
# Crear carpeta secrets/ y el archivo db_password.txt con la contraseña adentro
DB_PASSWORD_FILE=/run/secrets/db_password

# O, si no usás secrets (no recomendado), definir explícitamente una contraseña fuerte
# DB_PASSWORD=contraseña_super_fuerte_producción

#JWT
JWT_SECRET=prod_secret_change_me

# TTL del token en minutos
JWT_TTL_MIN=30

# Credenciales administrador inicial (opcional aquí; preferir secret separado)
ADMIN_EMAIL=admin@quepc.com
ADMIN_PASSWORD=change_me_super_secure
ADMIN_NAME=Administrador Produccion

# Base pública para assets (si se requiere override)
#API_ASSETS_BASE_URL=http://quepc.home
