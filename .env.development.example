# ============================================================
# Archivo de ejemplo para DESARROLLO
# Copiar como .env.development y modificar según tu entorno
# ============================================================

# --- Configuración de la API ---
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8080

# --- Conexión a la base de datos (consumida por la API) ---
DB_HOST=db                 # Nombre del servicio DB en docker-compose
DB_PORT=5432
DB_USER=postgres
DB_NAME=db_nahue
DB_SSLMODE=disable         # Deshabilitamos SSL en dev

# **IMPORTANTE**: Elegir solo una opción para la clave:
# O bien poner la clave directo:
# DB_PASSWORD=tu_password_dev
# O usar un docker secret (más seguro en producción):
# DB_PASSWORD_FILE=/run/secrets/db_password


# --- Configuración de Postgres (contenedor) ---
POSTGRES_USER=postgres
POSTGRES_DB=db_nahue

# Si usás secrets (recomendado incluso en dev para practicar buenas prácticas):
# OJO: Se sobreescribe por .env.development.secrets
POSTGRES_PASSWORD_FILE=/run/secrets/db_password


# --- Frontend (Vite) ---
# En desarrollo, el frontend apunta al API expuesto localmente
VITE_API_BASE_URL=http://localhost:8080/v1


# --- Opcional: Traefik para desarrollo ---
# Solo si levantás Traefik también en dev
# TRAEFIK_PORT=8081
