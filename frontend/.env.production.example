# Example production build-time variables for Vite
# Do NOT commit real secrets; frontend envs are baked into the bundle and public.
# Provide these via CI/CD environment instead of a .env file if you want to avoid
# using .env in production.
VITE_API_BASE_URL=https://api.example.com/v1
